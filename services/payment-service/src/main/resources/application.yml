spring:
  application:
    name: payment-service

server:
  port: 8082

pulseops:
  api:
    base-url: http://localhost:8080

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus

  endpoint:
    health:
      show-details: always