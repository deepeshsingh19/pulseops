spring:
  application:
    name: order-service

  main:
    web-application-type: servlet

server:
  port: 8081

pulseops:
  api:
    base-url: http://localhost:8080

management:
  endpoints:
    web:
      exposure:
        include: health,info